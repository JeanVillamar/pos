# Copiar como .env y completar con los valores reales.
# .env NO se sube a git.

# local | production — controla display_errors y otros comportamientos por entorno
APP_ENV=local

DB_DATABASE=u590035688_pos2
DB_USER=root
DB_PASS=CAMBIAR

# Token compartido entre CMS y API (header Authorization)
API_TOKEN=CAMBIAR_POR_UN_VALOR_ALEATORIO_LARGO
